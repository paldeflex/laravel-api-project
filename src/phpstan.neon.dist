includes:
    - vendor/nunomaduro/larastan/extension.neon

parameters:
    paths:
        - app
        - bootstrap
        - database
        - routes

    level: 7

    excludePaths:
        - vendor/*
        - storage/*
        - bootstrap/cache/*
        - database/migrations/*
